{
  "name": "todoapp",
  "scripts": {
    "e2e": "(cd e2e && npm run start)",
    "generate": "(cd api && npm run generate) && (cd web && npm run generate)",
    "postinstall": "(cd web && npm i) && (cd e2e && npm i) && (cd api && npm i)",
    "precommit": "(cd e2e && npm run precommit) && (cd api && npm run precommit)"
  },
  "dependencies": {},
  "devDependencies": {
    "husky": "^3.0.4"
  },
  "husky": {
    "hooks": {
      "pre-commit": "npm run precommit"
    }
  }
}
